pub type List<T> {
  Nil
  Cons(T, List<T>, str)
}

pub fn singleton<T>(value: T, label: str) -> List<T> {
  Cons<T>(value, Nil<T>(), label)
}

pub type Result<T, E> {
  Ok(T, E)
  Err(E)
}

pub fn success<T, E>(value: T, metadata: E) -> Result<T, E> { Ok<T, E>(value, metadata) }

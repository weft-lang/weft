pub type RightChoice {
  Same(bool)
}

pub fn read(choice: RightChoice) -> bool {
  match choice {
    Same(value) -> value
  }
}

impl RightChoice {
  pub fn origin_value() -> bool { true }

  pub fn same_value(self) -> bool {
    match self {
      Same(value) -> value
    }
  }
}

use runtime/rc_implicit_test.{RuntimeRcProbe}
use stdlib/result.{Result}

pub effect ManagedCallBoundary {
  fn exchange(value: RuntimeRcProbe) -> Result<i64, i64>
}

pub fn exchange(value: RuntimeRcProbe) -[ManagedCallBoundary]> Result<i64, i64> {
  ManagedCallBoundary.exchange(value)
}

-- A module with one public and one private type, for glob-import visibility.

pub type ShownType { value: i64 }

type HiddenType { value: i64 }

pub fn shown_value(shown: ShownType) -> i64 { shown.value }

-- runtime/syscall.weft -- sealed syscall and target-neutral process substrate

use runtime/memory.{mem_load64_at}
use runtime/unsafe.{*}

pub(package) fn syscall_number_ioctl() -> i64 { 54 }
pub(package) fn syscall_number_unlink() -> i64 { 10 }
pub(package) fn syscall_number_lstat() -> i64 { 340 }

pub(package) fn sys_raw(num: i64, a0: i64, a1: i64, a2: i64, a3: i64, a4: i64, a5: i64) -> i64 {
  handle unsafe_raw_syscall(num, a0, a1, a2, a3, a4, a5) {
    Unsafe.raw_syscall(n, b0, b1, b2, b3, b4, b5) -> resume(__syscall(n, b0, b1, b2, b3, b4, b5))
  }
}

pub(package) fn sys_exit(code: i64) -> never {
  __platform_exit(code)
  -- The kernel call cannot return on success. Keep the impossible error path
  -- divergent too, so the trusted signature remains an honest `never`.
  sys_exit(code)
}

pub(package) fn sys_read(fd: i64, buf: i64, len: i64) -> i64 {
  let status = __platform_read(fd, buf, len)
  if status < 0 { __platform_io_error(status) } else { status }
}

pub(package) fn sys_write(fd: i64, buf: i64, len: i64) -> i64 {
  let status = __platform_write(fd, buf, len)
  if status < 0 { __platform_io_error(status) } else { status }
}

pub(package) fn sys_secure_random_read(buf: i64, len: i64) -> i64 {
  __platform_secure_random_read(buf, len)
}

pub(package) fn sys_socket_af_inet() -> i64 { __platform_tcp_af_inet() }

pub(package) fn sys_socket_af_inet6() -> i64 { __platform_tcp_af_inet6() }

pub(package) fn sys_socket_sockaddr_prefix(family: i64, len: i64) -> i64 {
  __platform_tcp_sockaddr_prefix(family, len)
}

pub(package) fn sys_dns_udp_socket(family: i64) -> i64 {
  __platform_dns_udp_socket(family)
}

pub(package) fn sys_dns_tcp_socket(family: i64) -> i64 {
  __platform_dns_tcp_socket(family)
}

pub(package) fn sys_dns_connect(fd: i64, address: i64, len: i64) -> i64 {
  __platform_dns_connect(fd, address, len)
}

pub(package) fn sys_dns_set_receive_timeout(fd: i64, timeout_ms: i64) -> i64 {
  __platform_dns_set_receive_timeout(fd, timeout_ms)
}

pub(package) fn sys_open_read(path: i64) -> i64 {
  let status = __platform_open_read(path)
  if status < 0 { __platform_io_error(status) } else { status }
}

pub(package) fn sys_open_write_create_new(path: i64, mode: i64) -> i64 {
  let status = __platform_open_write_create_new(path, mode)
  if status < 0 { __platform_io_error(status) } else { status }
}

pub(package) fn sys_open_write_create_truncate(path: i64, mode: i64) -> i64 {
  let status = __platform_open_write_create_truncate(path, mode)
  if status < 0 { __platform_io_error(status) } else { status }
}

pub(package) fn sys_open_write_append(path: i64, mode: i64) -> i64 {
  let status = __platform_open_write_append(path, mode)
  if status < 0 { __platform_io_error(status) } else { status }
}

pub(package) fn sys_open_read_write_create_truncate(path: i64, mode: i64) -> i64 {
  let status = __platform_open_read_write_create_truncate(path, mode)
  if status < 0 { __platform_io_error(status) } else { status }
}

pub(package) fn sys_close(fd: i64) -> i64 {
  let status = __platform_close(fd)
  if status < 0 { __platform_io_error(status) } else { status }
}

pub(package) fn sys_remove(path: i64) -> i64 {
  let status = __platform_remove(path)
  if status < 0 { __platform_io_error(status) } else { status }
}

pub(package) fn sys_rename(from: i64, to: i64) -> i64 {
  let status = __platform_rename(from, to)
  if status < 0 { __platform_io_error(status) } else { status }
}

pub(package) fn sys_ioctl(fd: i64, request: i64, state: i64) -> i64 {
  sys_raw(syscall_number_ioctl(), fd, request, state, 0, 0, 0)
}

pub(package) fn sys_unlink(path: i64) -> i64 {
  sys_raw(syscall_number_unlink(), path, 0, 0, 0, 0, 0)
}

pub(package) fn sys_seek_start(fd: i64, offset: i64) -> i64 {
  let status = __platform_seek_start(fd, offset)
  if status < 0 { __platform_io_error(status) } else { status }
}

pub(package) fn sys_seek_current(fd: i64, offset: i64) -> i64 {
  let status = __platform_seek_current(fd, offset)
  if status < 0 { __platform_io_error(status) } else { status }
}

pub(package) fn sys_seek_end(fd: i64, offset: i64) -> i64 {
  let status = __platform_seek_end(fd, offset)
  if status < 0 { __platform_io_error(status) } else { status }
}

pub(package) fn sys_file_flush(fd: i64) -> i64 {
  let status = __platform_file_flush(fd)
  if status < 0 { __platform_io_error(status) } else { status }
}

pub(package) fn sys_dir_open(path: i64) -> i64 {
  let status = __platform_dir_open(path)
  if status < 0 { __platform_io_error(status) } else { status }
}

pub(package) fn sys_dir_read(fd: i64, buf: i64, len: i64, cookie: i64) -> i64 {
  let status = __platform_dir_read(fd, buf, len, cookie)
  if status < 0 { __platform_io_error(status) } else { status }
}

pub(package) fn sys_dir_entry_record_len(entry: i64) -> i64 {
  __platform_dir_entry_record_len(entry)
}

pub(package) fn sys_dir_entry_name(entry: i64) -> i64 {
  __platform_dir_entry_name(entry)
}

pub(package) fn sys_dir_entry_is_directory(entry: i64) -> i64 {
  __platform_dir_entry_is_directory(entry)
}

pub(package) fn sys_stat_scratch_size() -> i64 { 256 }
pub(package) fn sys_stat_mode(stat_buf: i64) -> i64 { mem_load64_at(stat_buf, 0) }
pub(package) fn sys_stat_size(stat_buf: i64) -> i64 { mem_load64_at(stat_buf, 8) }
pub(package) fn sys_stat_modified_seconds(stat_buf: i64) -> i64 { mem_load64_at(stat_buf, 16) }
pub(package) fn sys_stat_modified_nanos(stat_buf: i64) -> i64 { mem_load64_at(stat_buf, 24) }
pub(package) fn sys_stat_device(stat_buf: i64) -> i64 { mem_load64_at(stat_buf, 32) }
pub(package) fn sys_stat_inode(stat_buf: i64) -> i64 { mem_load64_at(stat_buf, 40) }

pub(package) fn sys_stat_follow(path: i64, stat_buf: i64) -> i64 {
  let status = __platform_stat_follow(path, stat_buf)
  if status < 0 { __platform_io_error(status) } else { status }
}

pub(package) fn sys_stat_nofollow(path: i64, stat_buf: i64) -> i64 {
  let status = __platform_stat_nofollow(path, stat_buf)
  if status < 0 { __platform_io_error(status) } else { status }
}

pub(package) fn sys_dir_mkdir(path: i64, mode: i64) -> i64 {
  let status = __platform_dir_mkdir(path, mode)
  if status < 0 { __platform_io_error(status) } else { status }
}

pub(package) fn sys_dir_remove_directory(path: i64) -> i64 {
  let status = __platform_dir_remove_directory(path)
  if status < 0 { __platform_io_error(status) } else { status }
}

pub(package) fn sys_dir_current(dot_path: i64, buf: i64, len: i64) -> i64 {
  let status = __platform_dir_current(dot_path, buf, len)
  if status < 0 { __platform_io_error(status) } else { status }
}

pub(package) fn sys_lstat(path: i64, stat_buf: i64) -> i64 {
  sys_raw(syscall_number_lstat(), path, stat_buf, 0, 0, 0, 0)
}

pub(package) fn sys_munmap(addr: i64, len: i64) -> i64 {
  __platform_unmap(addr, len)
}

pub(package) fn sys_map_rw_anonymous(addr: i64, len: i64) -> i64 {
  __platform_map_rw_anonymous(addr, len)
}

pub(package) fn sys_remap_rw_anonymous_fixed(addr: i64, len: i64) -> i64 {
  __platform_remap_rw_anonymous_fixed(addr, len)
}

pub(package) fn sys_native_host_target() -> i64 {
  __platform_native_host_target()
}

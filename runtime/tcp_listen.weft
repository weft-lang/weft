-- runtime/tcp_listen.weft — production inbound TCP authority

use runtime/tcp.{runtime_tcp_listen_safe}
use stdlib/tcp.{TcpListen}

--- Interpret TCP listener creation through the target platform.
pub implements TcpListen() {
  TcpListen.listen(address, options) -> resume(runtime_tcp_listen_safe(address, options))
}

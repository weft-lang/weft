-- runtime/tcp_connect.weft — production outbound TCP authority

use runtime/tcp.{runtime_tcp_connect_safe}
use stdlib/tcp.{TcpConnect}

--- Interpret outbound TCP connection requests through the target platform.
pub implements TcpConnect() {
  TcpConnect.connect(address, options) -> resume(runtime_tcp_connect_safe(address, options))
}

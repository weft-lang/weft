fn classify(n: i64) -> i64 {
  match n {
    x if x < 0 -> 0 - 1
    x if x == 0 -> 0
    x if x < 10 -> 1
    x if x < 100 -> 2
    _ -> 3
  }
}
fn abs(n: i64) -> i64 {
  match n { x if x < 0 -> 0 - x x -> x }
}

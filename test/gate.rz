fn abs(x: Int) -> Int {
  if x < 0 {
    0 - x
  } else {
    x
  }
}

fn classify(n: Int) -> Int {
  match n {
    0 -> 0,
    1 -> 1,
    _ -> 2
  }
}

fn main() -> Int {
  let a = abs(0 - 10)
  let b = classify(1)
  let d = match a {
    10 -> 1,
    _ -> 0
  }
  a + b + d + 30
}

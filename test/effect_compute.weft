-- Effect: resumed value used in computation
-- Expected exit code: 42
effect Config {
  fn base() -> i64
}

fn compute() -[Config]> i64 {
  let b = Config.base()
  b * 2 + b
}

fn main() {
  handle compute() {
    Config.base() -> resume(14)
  }
}

-- Method on variant type — access payload via impl method
-- Expected exit code: 42
type Shape { Circle(i64) }

impl Shape {
  fn radius(self: i64) -> i64 { __mem_load64(self + 8) }
}

fn main() -> i64 {
  let c = Circle(42)
  c.radius()
}

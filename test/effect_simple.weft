-- Simple effect: perform + handle with immediate resume
-- Expected exit code: 42
effect Counter {
  fn increment() -> Int
}

fn use_counter() -[Counter]> Int {
  Counter.increment()
}

fn main() {
  handle use_counter() {
    Counter.increment() -> resume(42)
  }
}

-- Closure / function-as-value via indirect call
fn double(x: Int) -> Int { x * 2 }
fn apply(f: Int, x: Int) -> Int { f(x) }

fn main() {
  -- apply(double, 21) = double(21) = 42
  apply(double, 21)
}

-- Expected exit code: 101
-- A negative element index panics instead of reading before the vector's data.

use stdlib/vector as vector
use stdlib/vector.{vector_get_in_bounds}

fn main() -> i64 {
  let mut values = vector.new<i64>()
  values.push(7)
  vector_get_in_bounds<i64>(values, 0 - 1)
}

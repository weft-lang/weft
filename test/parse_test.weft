-- Expected exit code: 0
use "runtime/alloc.weft"
use "runtime/memory.weft"
-- Test: read 1 byte from stdin

use "runtime/syscall.weft"

fn main() -> i64 {
  let arena = arena_new(128)
  let buf = arena_alloc(arena, 64, 8)
  sys_read(0, buf, 64)
  let result = mem_load8(buf)
  arena_free(arena)
  result
}

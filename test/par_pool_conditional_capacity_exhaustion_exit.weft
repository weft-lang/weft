-- Expected exit code: 70

use stdlib/par as par
use stdlib/result

fn select_scope(large: bool) -> i64 {
  par.with_pool(if large {
    par.pool_config(1, 1152921504606846975).unwrap()
  } else {
    par.pool_config(1, 1).unwrap()
  }, () => 42)
}

fn main() -> i64 {
  select_scope(true)
  0
}

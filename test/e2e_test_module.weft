-- Expected exit code: 42

use "stdlib/test.weft"

fn reusable_test_surface() -[Test]> i64 {
  Test.assert_eq(20 + 22, 42)
  42
}

fn main() -> i64 {
  handle reusable_test_surface() {
    Test.assert_eq(got, expected) -> resume(if got == expected { 0 } else { 1 })
  }
}

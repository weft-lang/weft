-- Test syscall intrinsic: write "hello!" to stdout
-- Expected: prints "hello!" to stdout, exits 42

fn main() -> i64 {
  let msg = "hello!"
  let ptr = __str_ptr(msg)
  __syscall(4, 1, ptr, 6, 0, 0, 0)
  42
}

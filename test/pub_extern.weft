-- Test: pub and extern fn declarations
-- pub is ignored (everything public in bootstrap)
-- extern fn has placeholder body
-- Expected exit code: 42

pub fn add(x: Int, y: Int) -> Int { x + y }

extern fn placeholder(n: Int) -> Int { n }

fn main() -> Int {
  add(20, 22)
}

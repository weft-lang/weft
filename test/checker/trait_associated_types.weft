trait AssocIter {
  type Item
  fn next(self: i64) -> i64
}

type AssocIterBox {
  v: i64
}

impl AssocIter for AssocIterBox {
  type Item = i64

  fn next(self: i64) -> i64 {
    __mem_load64(self) + 1
  }
}

fn assoc_iter_next<T: AssocIter>(x: T) -> i64 {
  x.next()
}

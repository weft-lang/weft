effect State {
  fn put(v: i64) -> i64
}

fn add(a: i64, b: i64) -> i64 {
  a + b
}

fn id<T>(x: T) -> T {
  x
}

fn apply2(f: (i64, i64) -> i64) -> i64 {
  f(20, 22)
}

fn main() -[State]> i64 {
  let a = add(20, 22)
  let b = id<i64>(0)
  let c = apply2(add)
  let _ = State.put(a + b + c)
  a
}

use "stdlib/result.weft"
use "stdlib/fail.weft"

fn id_i64(x: i64) -> i64 { x }

fn compute() -[Fail<i64>]> i64 {
  let _ = Ok<i64, i64>(42)?
  42
}

fn compute_arg() -[Fail<i64>]> i64 {
  id_i64(Ok<i64, i64>(42)?)
}

fn main() -> i64 {
  handle compute() {
    Fail.fail(e) -> e
  }
}

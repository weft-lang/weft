type LocalOption<T> {
  Some(T)
}

test "local declaration shadows the implicit prelude" {
  let value: LocalOption<i64> = Some<i64>(17)
  match value {
    Some(inner) -> Test.assert_eq(inner, 17)
  }
}

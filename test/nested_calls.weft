-- Expected exit code: 19
-- Deep nested calls testing register preservation across call boundaries
fn add(a: Int, b: Int) -> Int { a + b }
fn sub(a: Int, b: Int) -> Int { a - b }
fn mul(a: Int, b: Int) -> Int { a * b }

fn main() {
  -- add(mul(3, sub(10, 7)), mul(2, add(1, 4)))
  -- = add(mul(3, 3), mul(2, 5))
  -- = add(9, 10)
  -- = 19
  add(mul(3, sub(10, 7)), mul(2, add(1, 4)))
}

use runtime/rc_implicit_test.{*}

pub effect ModuleProbe {
  fn read() -> i64
  fn captured_count() -> i64
}

pub implements ModuleProbe(probe: RuntimeRcProbe) {
  ModuleProbe.read() -> resume(runtime_rc_probe_load(probe))
  ModuleProbe.captured_count() -> resume(runtime_rc_probe_count(probe))
}


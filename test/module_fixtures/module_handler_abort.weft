use runtime/rc_implicit_test.{*}

pub effect ModuleAbort {
  fn stop(value: i64) -> i64
}

pub implements ModuleAbort(probe: RuntimeRcProbe) {
  ModuleAbort.stop(value) -> runtime_rc_probe_count(probe) * 100 + value
}


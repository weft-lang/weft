--- Values accepted by a small host interface.
pub type Value = i64 | str

--- A pair that shares its element type.
pub type Pair<T> = (T, T)

--- A callback preserves the authority of its caller.
pub type Callback<E> = () -[E]> Value

--- Return the given value without a wrapper or conversion.
pub fn identity(value: Value) -> Value { value }

pub use test/module_fixtures/inherent_extension_types.{FirstValue, SecondValue, first, second}

impl FirstValue {
  --- Return this first value's payload.
  pub fn value(self) -> i64 {
    match self { FirstValue(value) -> value }
  }

  fn hidden(self) -> i64 {
    0
  }
}

impl SecondValue {
  --- Return this second value's payload.
  pub fn value(self) -> i64 {
    match self { SecondValue(value) -> value }
  }
}

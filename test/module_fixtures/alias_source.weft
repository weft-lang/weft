type Local { value: i64 }
pub type Item = Local
pub type Pair<T> = (T, T)
pub type Count = i64
pub fn item(value: i64) -> Item { Local { value: value } }
pub fn value(item: Item) -> Count { item.value }
pub fn pair<T>(left: T, right: T) -> Pair<T> { (left, right) }

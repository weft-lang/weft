-- Effect with arguments: perform passes args, handler receives them
-- Expected exit code: 42
effect Lookup {
  fn get(key: Int) -> Int
}

fn do_lookup() -> Int {
  Lookup.get(7)
}

fn main() {
  handle do_lookup() {
    Lookup.get(key) -> resume(key * 6)
  }
}

-- A bare generic nullary ctor with no usable expected type must still
-- be rejected (the expected type i64 gives no instantiation).
type Opt<T> {
  Just(T),
  Nothing
}

fn bad() -> i64 {
  Nothing
}

fn main() -> i64 { bad() }

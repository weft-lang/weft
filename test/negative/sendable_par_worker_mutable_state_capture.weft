use stdlib/vector as vector
use stdlib/vector.{*}
use stdlib/par as par
use stdlib/par.{*}

fn main() -> i64 {
  let state = vector.new<i64>()
  let values = [1]
  par.with_pool(2, 4, () => {
    vector_sum_i64(par.map<i64, i64>(values[..], x => x + vector_len<i64>(state)), 0)
  })
}

use stdlib/vector.{*}
use "stdlib/par.weft"

fn main() -> i64 {
  let state = vector_new<i64>()
  let values = [1]
  par_with_pthread_pool_i64(2, 4, () => {
    vector_sum_i64(par_map<i64, i64>(values[..], x => x + vector_len<i64>(state)), 0)
  })
}

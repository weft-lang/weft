use stdlib/iter as iter
use stdlib/vector.{Vector}

effect RecordChunk {
  fn size(value: i64) -> nil
}

fn record_chunk(chunk: owned Vector<i64>) -[RecordChunk]> nil {
  RecordChunk.size(chunk.len())
}

fn main() -> nil {
  let result = iter.each_chunk(iter.range(0, 3), 2, record_chunk)
  nil
}

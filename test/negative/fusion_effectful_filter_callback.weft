use "stdlib/list.weft"

effect Log {
  fn hit() -> i64
}

fn add(acc: i64, x: i64) -> i64 { acc + x }

fn main() -[Log]> i64 {
  list_fold<i64, i64>(
    list_filter<i64>(list_cons<i64>(1, list_nil<i64>()), x => Log.hit()),
    0,
    add
  )
}

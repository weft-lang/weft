use stdlib/list.{*}

effect Log {
  fn hit() -> i64
}

fn add(acc: i64, x: i64) -> i64 { acc + x }

fn main() -[Log]> i64 {
  let filtered = Cons<i64>(1, Nil<i64>()).filter(x => Log.hit())
  filtered.fold<i64>(0, add)
}

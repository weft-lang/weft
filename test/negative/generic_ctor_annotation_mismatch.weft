-- Argument-driven inference must not override a conflicting annotation:
-- Just("nope") solves T = str, which does not match Opt<i64>.
type Opt<T> {
  Just(T),
  Nothing
}

fn main() -> i64 {
  let o: Opt<i64> = Just("nope")
  1
}

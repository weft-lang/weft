use "runtime/alloc.weft"

effect RegionProbe {
  fn get() -> i64
}

fn main() -> i64 {
  let arena = arena_new(64)
  with_scoped_arena_alloc(arena, () => {
    handle {
      let p = Alloc.alloc(8, 8)
      p
    } {
      RegionProbe.get() -> resume(0)
    }
  })
}

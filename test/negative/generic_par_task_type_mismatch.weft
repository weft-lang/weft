use stdlib/par as par
use stdlib/par.{*}
use stdlib/string.{*}

fn main() -> i64 {
  par.with_sequential(() => {
    let task = par.fork<i64>(() => 42)
    let wrong: unique ParTask<str> = task
    str_len(wrong.join())
  })
}

use stdlib/spawn as spawn
use stdlib/vector as vector
use stdlib/vector.{*}

fn bad() -> i64 {
  spawn.with_sequential(() => {
    let task = spawn.spawn(() => vector.new<i64>())
    vector_len<i64>(spawn.join(task))
  })
}

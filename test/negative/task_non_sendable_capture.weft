use stdlib/task as task
fn inspect_dynamic(value: any) -> i64 { 0 }

fn bad(state: any) -> i64 {
  task.with_sequential(() => {
    let child = task.spawn(() => inspect_dynamic(state))
    child.join()
  })
}

use stdlib/task as task
use stdlib/vector as vector
use stdlib/vector.{*}

fn bad() -> i64 {
  let state = vector.new<i64>()
  task.with_sequential(() => {
    let child = task.spawn(() => vector_len<i64>(state))
    child.join()
  })
}

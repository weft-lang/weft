use runtime/alloc.{*}

effect RegionProbe {
  fn get() -> i64
}

fn main() -> i64 {
  let arena = arena_new(64)
  with_scoped_arena_alloc(arena, () => {
    handle RegionProbe.get() {
      RegionProbe.get() -> {
        let p = Alloc.alloc(8, 8)
        resume(p)
      }
    }
  })
}

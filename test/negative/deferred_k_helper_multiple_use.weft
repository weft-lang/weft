effect DeferredK {
  @deferred fn get() -> i64
}

fn consume(k: Continuation<i64, i64>, value: i64) -> i64 {
  k(value)
}

fn main() -> i64 {
  handle DeferredK.get() * 2 {
    DeferredK.get() with k -> {
      consume(k, 10) + consume(k, 20)
    }
  }
}

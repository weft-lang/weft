use stdlib/par.{*}

effect Log {
  fn hit() -> i64
}

fn noisy(x: i64) -[Log]> i64 {
  Log.hit() + x
}

fn main() -[Par]> i64 {
  let values = [1]
  vector_sum_i64(par_map<i64, i64>(values[..], noisy), 0)
}

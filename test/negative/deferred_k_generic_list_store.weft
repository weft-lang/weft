type KList<T> {
  KNil,
  KCons(T, KList<T>)
}

effect DeferredK {
  @deferred fn get() -> i64
}

fn main() -> i64 {
  handle DeferredK.get() {
    DeferredK.get() with k -> {
      let stored = KCons<Continuation<i64, i64>>(k, KNil<Continuation<i64, i64>>())
      0
    }
  }
}

use stdlib/http.{HttpLimits, HttpRequestHead}
use stdlib/http_endpoint.{HttpClientBegin, HttpServer, http_client_reuse}
use stdlib/http_stream.{HttpConnection}
use stdlib/tcp.{TcpStreamIO}
use stdlib/url.{Url}

fn reuse_with_server_authority(
  url: Url,
  connection: owned HttpConnection,
  request: HttpRequestHead,
  limits: HttpLimits
) -[HttpServer, TcpStreamIO]> HttpClientBegin {
  http_client_reuse(url, connection, request, limits)
}

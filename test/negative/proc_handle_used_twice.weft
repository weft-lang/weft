use runtime/process.{*}

fn bad(resource: owned ProcHandle) -[Proc]> i64 {
  let first = Proc.wait(resource)
  let second = Proc.wait(resource)
  first.status + second.status
}

fn main() -> i64 { 0 }

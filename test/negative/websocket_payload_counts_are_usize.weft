use stdlib/websocket.{WebSocketFrameHeader, WebSocketPayloadDecoder}

fn header_count(header: WebSocketFrameHeader) -> i64 { header.payload_bytes() }

fn remaining_count(decoder: WebSocketPayloadDecoder) -> i64 { decoder.remaining() }

fn main() -> i64 { 0 }

use stdlib/iter as iter
use stdlib/option.{*}

effect Log {
  fn hit() -> i64
}

fn main() -[Log]> nil {
  let values = iter.filter_map(
    iter.range(0, 3),
    value => Some<i64>(value + Log.hit())
  )
  values.close()
}

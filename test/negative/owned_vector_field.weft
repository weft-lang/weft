use stdlib/drop.{Drop}
use stdlib/vector/handles.{Vector}

type OwnedVectorToken = opaque i64

impl Drop for OwnedVectorToken {
  fn drop(self: i64) -> i64 { 0 }
}

type BadOwnedVector {
  handles: Vector<owned OwnedVectorToken>
}

fn main() -> i64 {
  0
}

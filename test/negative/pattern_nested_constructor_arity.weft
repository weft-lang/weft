type NestedArityInner {
  Inner(i64),
}

type NestedArityOuter {
  Wrap(NestedArityInner),
  Stop,
}

fn main(value: NestedArityOuter) -> i64 {
  match value {
    Wrap(Inner(left, right)) -> left + right
    _ -> 0
  }
}

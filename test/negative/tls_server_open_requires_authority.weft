use stdlib/bytes.{Bytes}
use stdlib/result.{Result}
use stdlib/tls.{TlsError, TlsSession, tls_server_open}

fn open_without_authority(
  certificate: Bytes,
  private_key: Bytes
) -> Result<owned TlsSession, TlsError> {
  tls_server_open(certificate, private_key)
}

trait Ord {
  fn compare(self: Self, other: Self) -> i64
}

type LegacyRank {
  LegacyRank(i64)
}

impl Ord for LegacyRank {
  fn compare(self: Self, other: Self) -> i64 { 0 }
}

fn main() -> bool {
  LegacyRank(1) < LegacyRank(2)
}

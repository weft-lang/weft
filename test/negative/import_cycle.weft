-- 4a S6 (kernel §12.5): import cycles get a diagnostic instead of the old
-- silent dedup. Diamond re-imports remain silently deduplicated.
use test/negative/import_cycle_helper.{*}

fn cycle_a() -> i64 {
  1
}

fn main() -> i64 {
  cycle_a()
}

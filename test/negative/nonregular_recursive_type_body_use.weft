use stdlib/vector/handles.{Vector}

type Chain<T> { ChainEnd(T), ChainDeeper(Chain<Vector<T>>) }

fn depth<T>(chain: Chain<T>) -> i64 {
  match chain {
    ChainEnd(value) -> 0
    ChainDeeper(deeper) -> 1 + depth<Vector<T>>(deeper)
  }
}

fn main() -> i64 { depth<i64>(ChainEnd<i64>(3)) }

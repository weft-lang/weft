effect DeferredK {
  @deferred fn get() -> i64
}

fn apply_saved(f: (i64) -> i64) -> i64 {
  f(1)
}

fn main() -> i64 {
  handle DeferredK.get() {
    DeferredK.get() with k -> {
      let f = x => k(x)
      apply_saved(f)
    }
  }
}

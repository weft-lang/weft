-- set-theoretic surface: call-site argument labels used to be parsed and silently
-- discarded. They are fenced until named-argument semantics land.
fn add(a: i64, b: i64) -> i64 {
  a + b
}

fn main() -> i64 {
  add(a: 1, b: 2)
}

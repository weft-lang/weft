trait AssociatedFactory {
  fn make(value: i64) -> Self
}

type ReceiverBox {
  value: i64
}

impl AssociatedFactory for ReceiverBox {
  fn make(self: i64) -> Self {
    ReceiverBox { value: self }
  }
}

fn main() -> i64 { 0 }

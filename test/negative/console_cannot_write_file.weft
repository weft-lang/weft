use stdlib/bytes.{bytes_empty}
use stdlib/console.{ConsoleWrite}
use stdlib/file.{file_write_all}
use stdlib/io_types.{IoError}
use stdlib/path.{Path}
use stdlib/result.{Result}

fn rejected(path: Path) -[ConsoleWrite]> Result<usize, IoError> {
  file_write_all(path, bytes_empty())
}

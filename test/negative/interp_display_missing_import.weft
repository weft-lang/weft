-- String interpolation desugars to .display(); without the Display
-- impls imported the diagnostic must hint at stdlib/display.weft
-- instead of a bare "unknown method".
fn main() -> i64 {
  let n = 42
  let s = "value: {n}"
  0
}

use stdlib/vector as vector
use stdlib/vector/handles.{Vector}

fn grow<T>(n: i64, value: T) -> i64 {
  if n == 0 { 0 } else {
    let mut wrapped = vector.new<T>()
    wrapped.push(value)
    1 + grow<Vector<T>>(n - 1, wrapped)
  }
}

fn main() -> i64 { grow<i64>(3, 1) }

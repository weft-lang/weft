use stdlib/task as task
use stdlib/vector as vector
use stdlib/vector.{*}

fn bad() -> i64 {
  task.with_sequential(() => {
    let child = task.spawn(() => vector.new<i64>())
    vector_len<i64>(task.join(child))
  })
}

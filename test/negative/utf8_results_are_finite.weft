use stdlib/option.{Option}
use stdlib/utf8 as utf8
use stdlib/utf8.{Utf8Decoded, Utf8Malformed}

fn signed_width() -> Option<i64> { utf8.encoded_len(65) }

fn signed_offset() -> i64 {
  match utf8.decode_next("A", 0) {
    Utf8Decoded(next, _) -> next
    Utf8Malformed(offset) -> offset
  }
}

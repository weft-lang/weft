use stdlib/iter as iter
use stdlib/iter.{IntoIterator, Iterator}

fn count<C: IntoIterator>(values: C) -> i64 {
  let mut total = 0
  for _ in values { total = total + 1 }
  total
}

fn main() -> i64 {
  count<Iterator<i64>>(iter.range(0, 3))
}

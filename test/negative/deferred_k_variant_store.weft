type KVariantStore {
  KNone,
  KSome(Continuation<i64, i64>)
}

effect DeferredK {
  @deferred fn get() -> i64
}

fn main() -> i64 {
  handle DeferredK.get() {
    DeferredK.get() with k -> {
      let stored = KSome(k)
      0
    }
  }
}

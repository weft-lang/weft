-- 4k: a qualified perform names the full effect atom, so Box<i64> cannot
-- be charged to a function that only declares Box<str>.
effect Box<T> {
  fn get() -> T
}

fn f() -[Box<str>]> i64 {
  Box<i64>.get()
}

fn main() -> i64 {
  0
}

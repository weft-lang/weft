use "stdlib/generator.weft"

fn consume_unknown_producer(producer: () -[GeneratorYield]> i64) -> i64 {
  let g = generator_i64_new(producer)
  generator_i64_is_done(g)
}

fn main() -> i64 {
  consume_unknown_producer(() => 0)
}

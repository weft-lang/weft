-- Annotations name associated types like signatures do.

trait Level {
  type Child: Level
  fn size(self) -> i64
}

fn ghost<T: Level>(node: T) -> i64 {
  let missing: T.Parent? = nil
  node.size()
}

fn main() -> i64 { 0 }

-- A misspelt type in an associated constraint is an unknown type.

trait Level {
  type Child: Level
  fn size(self) -> i64
}

type Branch { size: i64 }

fn child_size<T: Level & { type Child = Brnch }>(node: T) -> i64 { node.size() }

fn main() -> i64 { 0 }

use stdlib/spawn as spawn
use stdlib/vector.{*}

fn bad() -> i64 {
  let state = vector_new<i64>()
  spawn.with_sequential(() => {
    let task = spawn.spawn(() => vector_len<i64>(state))
    spawn.join(task)
  })
}

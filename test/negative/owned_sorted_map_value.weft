use stdlib/drop.{Drop}
use stdlib/sorted_map.{SortedMap}

type ContainedToken = opaque i64

impl Drop for ContainedToken {
  fn drop(self: i64) -> i64 { 0 }
}

type BadHolder { handles: SortedMap<i64, owned ContainedToken> }

fn main() -> i64 { 0 }

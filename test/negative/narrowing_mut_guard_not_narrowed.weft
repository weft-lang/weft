-- Guards must not narrow mutable bindings: an assignment between the
-- guard and the use could restore nil.
fn use_str(s: str) -> i64 { __str_len(s) }

fn bad(y: str | nil) -> i64 {
  let mut x = y
  if x != nil {
    x = y
    use_str(x)
  } else { 0 }
}

fn main() -> i64 { bad("hi") }

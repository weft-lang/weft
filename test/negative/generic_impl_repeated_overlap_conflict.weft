type PairBox<A, B> { PairBox(A, B) }
trait Identity { fn identity(self) -> Self }

impl<T> Identity for PairBox<T, T> {
  fn identity(self) -> Self { self }
}

impl<U, V> Identity for PairBox<U, V> {
  fn identity(self) -> Self { self }
}

fn main() -> i64 { 0 }

-- 4c: two atoms with the same head but different instantiations in one
-- signature are fenced — perform-site disambiguation syntax is
-- undesigned (kernel §7.1 ambiguity, recorded in the brief).
effect Box<T> {
  fn get() -> T
}

fn f() -[Box<i64>, Box<str>]> i64 {
  1
}

fn main() -> i64 {
  0
}

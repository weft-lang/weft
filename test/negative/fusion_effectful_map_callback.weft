use "stdlib/list.weft"

effect Log {
  fn hit() -> i64
}

fn add(acc: i64, x: i64) -> i64 { acc + x }

fn main() -[Log]> i64 {
  list_fold<i64, i64>(
    list_map<i64, i64>(Cons<i64>(1, Nil), x => Log.hit() + x),
    0,
    add
  )
}

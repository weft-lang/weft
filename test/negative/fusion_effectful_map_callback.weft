use stdlib/list.{*}

effect Log {
  fn hit() -> i64
}

fn add(acc: i64, x: i64) -> i64 { acc + x }

fn main() -[Log]> i64 {
  let mapped = Cons<i64>(1, Nil<i64>()).map<i64>(x => Log.hit() + x)
  mapped.fold<i64>(0, add)
}

-- Conflicting argument types for the same type parameter must abort
-- inference and be rejected, not silently pick one binding.
type Both<T> {
  Pair2(T, T)
}

fn main() -> i64 {
  let b = Pair2(1, "mixed")
  1
}

effect DeferredK {
  @deferred fn get() -> i64
}

fn main() -> i64 {
  let slot = __bump_alloc(8)
  handle DeferredK.get() {
    DeferredK.get() with k -> {
      __mem_store64(slot, k)
      0
    }
  }
}

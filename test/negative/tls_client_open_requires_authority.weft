use stdlib/bytes.{Bytes}
use stdlib/result.{Result}
use stdlib/tls.{TlsError, TlsSession, tls_client_open}
use stdlib/url.{UrlHost}

fn open_without_authority(
  host: UrlHost,
  roots: Bytes
) -> Result<owned TlsSession, TlsError> {
  tls_client_open(host, roots)
}

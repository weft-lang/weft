use stdlib/iter as iter
use stdlib/vector as vector
use stdlib/vector.{Vector}
use stdlib/string

type Entry { text: str }

fn main() -> i64 {
  let mut values = vector.new<Entry>()
  values.push(Entry { text: "typed" })
  let position = iter.position(values, (entry: str) => entry.len() > 0)
  0
}

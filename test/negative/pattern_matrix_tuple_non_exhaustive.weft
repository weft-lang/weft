type MatrixTupleBit {
  MatrixTupleZero,
  MatrixTupleOne,
}

fn bad(value: (MatrixTupleBit, MatrixTupleBit)) -> i64 {
  match value {
    (MatrixTupleZero, _) -> 0
    (MatrixTupleOne, MatrixTupleZero) -> 1
  }
}

fn main() -> i64 { bad((MatrixTupleOne, MatrixTupleOne)) }

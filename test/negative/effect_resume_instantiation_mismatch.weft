-- parameterized effects: resume checks against the instantiated op return type.
effect Box<T> {
  fn get() -> T
}

fn use_box() -[Box<str>]> str {
  Box.get()
}

fn main() -> i64 {
  let s = handle use_box() {
    Box.get() -> resume(42)
  }
  0
}

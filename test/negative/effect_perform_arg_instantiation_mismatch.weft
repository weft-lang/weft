-- parameterized effects: perform args check against the INSTANTIATED op signature — put(42)
-- on Box<str> is a type error, not an opaque-TypeVar pass.
effect Box<T> {
  fn put(v: T) -> nil
}

fn f() -[Box<str>]> i64 {
  Box.put(42)
  0
}

fn main() -> i64 {
  0
}

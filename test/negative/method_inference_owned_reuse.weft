use test/negative_support/method_inference_support.{InferenceCheckHost}
use stdlib/vector as vector
use stdlib/vector.{Vector}
fn main() -> i64 {
  let values = vector.new<i64>()
  let moved = InferenceCheckHost<bool>(true).choose(values)
  values.len()
  moved.len()
  0
}

-- Nullary qualified construction (field-access shape) gets the same
-- targeted diagnostic instead of "unknown identifier" on the type name.
type Shape {
  Circle(i64),
  Point
}

fn main() -> i64 {
  let p = Shape.Point
  0
}

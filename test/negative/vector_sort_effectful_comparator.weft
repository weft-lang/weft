use "stdlib/vector.weft"

effect SortNoise { fn compare(left: i64, right: i64) -> i64 }

fn noisy_compare(left: i64, right: i64) -[SortNoise]> i64 {
  SortNoise.compare(left, right)
}

fn main() -> i64 {
  vector_sort_by<i64>(vector_new<i64>(), noisy_compare)
  0
}

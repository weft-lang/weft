use stdlib/par as par
use stdlib/par.{*}
use stdlib/vector as vector
use stdlib/vector.{*}

fn main() -> i64 {
  let state = vector.new<i64>()
  par.with_pool(2, 4, () => {
    let task = par.fork<i64>(() => vector_len<i64>(state))
    task.join()
  })
}

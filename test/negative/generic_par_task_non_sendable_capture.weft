use "stdlib/par.weft"
use stdlib/vector.{*}

fn main() -> i64 {
  let state = vector_new<i64>()
  par_with_pthread_pool_i64(2, 4, () => {
    let task = par_spawn<i64>(() => vector_len<i64>(state))
    par_await<i64>(task)
  })
}

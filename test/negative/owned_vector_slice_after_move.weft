use stdlib/vector as vector
use stdlib/vector.{Vector}

fn consume(values: owned Vector<i64>) -> nil { nil }
fn observe(values: [i64]) -> usize { values.len }

fn bad() -> usize {
  let values = vector.new<i64>()
  consume(values)
  observe(values[..])
}

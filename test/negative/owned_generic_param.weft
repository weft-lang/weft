use stdlib/drop.{Drop}
use stdlib/vector/handles.{Vector}

type OwnedGenericToken = opaque i64

impl Drop for OwnedGenericToken {
  fn drop(self: i64) -> i64 { 0 }
}

fn bad(xs: Vector<owned OwnedGenericToken>) -> i64 {
  0
}

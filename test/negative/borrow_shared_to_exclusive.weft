use stdlib/drop.{*}

type BorrowForwardToken = opaque i64

impl Drop for BorrowForwardToken {
  fn drop(self) -> nil { nil }
}

fn mutate(token: borrow mut BorrowForwardToken) -> i64 { 0 }
fn bad(token: borrow BorrowForwardToken) -> i64 { mutate(token) }

-- Residual-set exhaustiveness: str | nil minus str leaves nil — the
-- match must not check without a nil (or catch-all) arm.
fn bad(x: str | nil) -> i64 {
  match x {
    s: str -> 1
  }
}

fn main() -> i64 { 0 }

-- A string interpolation names an identifier the program never binds. The
-- diagnostic reports that identifier rather than a missing effect operation
-- on the synthesized display call.

fn main() -> i64 {
  let text = "type Program<T> = {value: T}"
  __str_len(text)
}

type OverlapBox<T> { OverlapBox(T) }
trait Identity { fn identity(self) -> Self }

impl<T> Identity for OverlapBox<T> {
  fn identity(self) -> Self { self }
}

impl Identity for OverlapBox<i64> {
  fn identity(self) -> Self { self }
}

fn main() -> i64 { 0 }

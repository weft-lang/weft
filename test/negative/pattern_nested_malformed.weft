type NestedMalformedInner {
  Inner(i64),
}

type NestedMalformedOuter {
  Wrap(NestedMalformedInner),
  Stop,
}

fn main(value: NestedMalformedOuter) -> i64 {
  match value {
    Wrap(Inner(n) n) -> n
    _ -> 0
  }
}

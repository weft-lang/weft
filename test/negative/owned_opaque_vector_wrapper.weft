use stdlib/drop.{Drop}
use stdlib/vector/handles.{Vector}

type ContainedToken = opaque i64

impl Drop for ContainedToken {
  fn drop(self: i64) -> i64 { 0 }
}

type Bag<T> = opaque Vector<T>

type BadHolder { handles: Bag<owned ContainedToken> }

fn main() -> i64 { 0 }

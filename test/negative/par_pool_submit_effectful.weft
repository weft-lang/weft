use stdlib/par.{*}

effect Log {
  fn hit() -> i64
}

fn noisy(x: i64) -[Log]> i64 {
  Log.hit() + x
}

fn main() -> i64 {
  let pool = par_pthread_pool_start(2, 4)
  par_pthread_pool_submit(pool, noisy, 1)
}

effect DeferredK {
  @deferred fn get() -> i64
}

fn generic_id<T>(x: T) -> T {
  x
}

fn main() -> i64 {
  handle DeferredK.get() {
    DeferredK.get() with k -> {
      let saved = generic_id<Continuation<i64, i64>>(k)
      saved(1)
    }
  }
}

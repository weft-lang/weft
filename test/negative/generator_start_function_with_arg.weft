use stdlib/generator as generator
use stdlib/generator.{*}

fn producer_with_arg(x: i64) -[GeneratorYield]> i64 {
  x
}

fn main() -> i64 {
  let g = generator.start<i64, i64>(producer_with_arg)
  g.is_done()
}

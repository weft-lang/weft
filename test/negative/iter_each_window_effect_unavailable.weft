use stdlib/iter as iter

effect RecordWindow {
  fn size(value: usize) -> nil
}

fn record_window(window: borrow [i64]) -[RecordWindow]> nil {
  RecordWindow.size(window.len)
}

fn main() -> nil {
  let result = iter.each_window(iter.range(0, 3), 2, record_window)
  nil
}

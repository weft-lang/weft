effect DeferredK {
  @deferred fn get() -> i64
}

fn go() -[DeferredK]> i64 {
  DeferredK.get()
}

fn main() -> i64 {
  handle go() {
    DeferredK.get() with k -> {
      let value = k(41)
      value + 1
    }
  }
}

use stdlib/iter as iter

effect Log {
  fn hit() -> i64
}

fn main() -[Log]> nil {
  let values = iter.flat_map(
    iter.range(0, 3),
    value => iter.once(value + Log.hit())
  )
  values.close()
}

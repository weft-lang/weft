use stdlib/vector as vector

fn main() -> i64 {
  let mut values = vector.new<i64>()
  values.push(41)
  let mut total = 0
  for value in values {
    total = total + value
  }
  total + values.len()
}

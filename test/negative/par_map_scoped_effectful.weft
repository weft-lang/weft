use stdlib/par as par
use stdlib/par.{*}

effect Log {
  fn hit() -> i64
}

fn noisy(x: i64) -[Log]> i64 {
  Log.hit() + x
}

fn main() -> i64 {
  let values = [1]
  par.with_pool(2, 4, () => vector_sum_i64(par.map<i64, i64>(values[..], noisy), 0))
}

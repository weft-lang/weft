use "stdlib/par.weft"

effect Log {
  fn hit() -> i64
}

fn noisy(x: i64) -[Log]> i64 {
  Log.hit() + x
}

fn main() -> i64 {
  par_with_pthread_pool_i64(2, 4, () => par_map(list_cons<i64>(1, list_nil<i64>()), noisy))
}

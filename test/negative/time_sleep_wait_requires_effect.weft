use stdlib/result.{Result}
use stdlib/time as time
use stdlib/time/sleep as sleep
use stdlib/time/sleep.{SleepError}

fn bad() -> Result<nil, SleepError> {
  sleep.wait(time.milliseconds(1))
}

fn main() -> i64 { 0 }

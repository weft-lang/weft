use stdlib/par as par
use stdlib/result
use stdlib/vector as vector
fn bad() -> usize {
  let output = par.with_pool(par.pool_config(2, 4).unwrap(), () => vector.new<i64>())
  let moved = output
  output.len()
}

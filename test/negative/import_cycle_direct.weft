-- A module is part of the active logical load graph before its first import.
-- A direct self-edge must diagnose without rereading the source.
use test/negative/import_cycle_direct.{*}

fn main() -> i64 {
  0
}

use stdlib/drop.{*}

type BorrowImmutableToken = opaque i64

impl Drop for BorrowImmutableToken {
  fn drop(self) -> nil { nil }
}

fn mutate(token: borrow mut BorrowImmutableToken) -> i64 { 0 }

fn bad() -> i64 {
  let token: owned BorrowImmutableToken = BorrowImmutableToken(1)
  mutate(token)
}

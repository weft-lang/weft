effect DeferredK {
  @deferred fn get() -> i64
}

fn main() -> i64 {
  handle if true { DeferredK.get() * 2 } else { 0 } {
    DeferredK.get() with k -> {
      let resumed = k(20)
      resumed + 2
    }
  }
}

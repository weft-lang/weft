effect DeferredK {
  @deferred fn get() -> i64
}

fn main() -> i64 {
  handle {
    let mut total = 0
    while total < 2 {
      total = total + DeferredK.get()
    }
    total
  } {
    DeferredK.get() with k -> {
      let resumed = k(20)
      resumed + 2
    }
  }
}

effect DeferredK {
  @deferred fn get() -> i64
}

fn main() -> i64 {
  handle {
    let first = DeferredK.get()
    let second = DeferredK.get()
    first + second
  } {
    DeferredK.get() with k -> {
      let resumed = k(20)
      resumed + 2
    }
  }
}

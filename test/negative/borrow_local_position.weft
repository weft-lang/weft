use stdlib/drop.{*}

type Token = opaque i64

impl Drop for Token {
  fn drop(self) -> nil { nil }
}

fn bad(token: owned Token) -> i64 {
  let alias: borrow Token = token
  0
}

fn main() -> i64 { 0 }

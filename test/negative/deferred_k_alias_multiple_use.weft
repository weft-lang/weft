effect DeferredK {
  @deferred fn get() -> i64
}

fn main() -> i64 {
  handle DeferredK.get() {
    DeferredK.get() with k -> {
      let saved = k
      let first = saved(1)
      saved(first)
    }
  }
}

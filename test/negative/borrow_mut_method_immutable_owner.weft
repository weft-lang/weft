use stdlib/drop.{*}

type BorrowMethodToken = opaque i64

impl Drop for BorrowMethodToken {
  fn drop(self) -> nil { nil }
}

impl BorrowMethodToken {
  fn mutate(self: borrow mut Self) -> i64 { 0 }
}

fn bad() -> i64 {
  let token: owned BorrowMethodToken = BorrowMethodToken(1)
  token.mutate()
}

effect Log {
  fn hit() -> i64
}

type EffectRecord {
  v: i64
}

fn apply_record(f: (i64) -> EffectRecord, x: i64) -> EffectRecord {
  f(x)
}

fn bad() -[Log]> EffectRecord {
  let f = x => EffectRecord { v: Log.hit() + x }
  apply_record(f, 41)
}

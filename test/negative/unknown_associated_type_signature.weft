-- No trait carried by T declares an associated type named Parent.

trait Level {
  type Child: Level
  fn child(self) -> Self.Child
  fn size(self) -> i64
}

fn ghost<T: Level>(node: T, other: T.Parent) -> i64 { node.size() }

fn main() -> i64 { 0 }

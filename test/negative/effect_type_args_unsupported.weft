-- 4a S1 (law L2): parameterized effects are owned by brief 4c. Before the
-- fence -[State<SymTab>]> silently parsed as the two-atom set {State, SymTab}.
effect State {
  fn get() -> i64
}

fn f() -[State<i64>]> i64 {
  1
}

fn main() -> i64 {
  0
}

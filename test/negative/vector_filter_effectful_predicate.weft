use "stdlib/vector.weft"

effect FilterNoise { fn accept(value: i64) -> bool }

fn noisy_accept(value: i64) -[FilterNoise]> bool {
  FilterNoise.accept(value)
}

fn main() -> i64 {
  vector_filter<i64>(vector_new<i64>(), noisy_accept)
  0
}

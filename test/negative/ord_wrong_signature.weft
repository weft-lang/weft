trait Ord {
  fn compare(self: Self) -> bool
}

type WrongOrd {
  WrongOrd(i64)
}

impl Ord for WrongOrd {
  fn compare(self: Self) -> bool { true }
}

fn main() -> bool {
  WrongOrd(1) < WrongOrd(2)
}

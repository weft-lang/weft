type BorrowMutableManagedValue {
  text: str
}

fn mutate(value: borrow mut BorrowMutableManagedValue) -> i64 { 0 }

fn main() -> i64 {
  let mut value = BorrowMutableManagedValue { text: "shared" }
  mutate(value)
}

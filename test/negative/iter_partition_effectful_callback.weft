use stdlib/iter as iter

effect Log {
  fn keep(value: i64) -> bool
}

fn main() -[Log]> i64 {
  let (accepted, rejected) = iter.partition(iter.range(0, 3), value => Log.keep(value))
  accepted.len() + rejected.len()
}

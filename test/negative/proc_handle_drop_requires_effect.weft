use runtime/process.{*}

fn bad() -> i64 {
  let resource: owned ProcHandle = ProcHandle {
    pid: 0,
    stdout_fd: 0 - 1,
    stderr_fd: 0 - 1,
    spawn_error: 1
  }
  0
}

fn main() -> i64 { 0 }

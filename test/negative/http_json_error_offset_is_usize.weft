use stdlib/http/json.{HttpJsonError, HttpJsonInvalidDocument}

fn position(error: HttpJsonError) -> i64 {
  let fallback: usize = 0
  match error { HttpJsonInvalidDocument(at) -> at _ -> fallback }
}

fn main() -> i64 { position(HttpJsonInvalidDocument(0)) }

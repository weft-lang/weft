-- 4c: at most one return clause per handler.
effect Ask {
  fn ask() -> i64
}

fn f() -[Ask]> i64 {
  Ask.ask()
}

fn main() -> i64 {
  handle f() {
    Ask.ask() -> resume(1)
    return v -> v
    return w -> w
  }
}

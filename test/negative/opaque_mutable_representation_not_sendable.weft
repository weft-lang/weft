use stdlib/vector.{vector_new}
use stdlib/vector/handles.{Vector}

type MutableOpaque = opaque Vector<i64>

fn require_sendable<T: Sendable>(value: T) -> T {
  value
}

fn rejected() -> MutableOpaque {
  require_sendable<MutableOpaque>(MutableOpaque(vector_new<i64>()))
}

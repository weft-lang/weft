-- A glob import brings in a module's public surface only.

use module_fixtures/private_type_holder.{*}

fn read(hidden: HiddenType) -> i64 { hidden.value }

fn main() -> i64 { shown_value(ShownType { value: 1 }) }

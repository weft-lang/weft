use stdlib/iter as iter

fn main() -> usize {
  let source = iter.range(0, 3)
  let mapped = iter.flat_map(source, value => iter.once(value))
  let original_count = iter.count(source)
  original_count + iter.count(mapped)
}

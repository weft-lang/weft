-- A nil arm on a scrutinee that is provably never nil is dead — and
-- under the old binding-pattern reading it silently swallowed every
-- value, so it is rejected outright.
fn bad(x: i64) -> i64 {
  match x {
    nil    -> 1
    v: i64 -> v
  }
}

fn main() -> i64 { 0 }

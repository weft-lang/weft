use stdlib/iter as iter
use stdlib/option.{*}

effect Log {
  fn hit() -> i64
}

fn main() -[Log]> Option<i64> {
  iter.reduce<i64>(iter.range(0, 3), (acc: i64, value: i64) => acc + value + Log.hit())
}

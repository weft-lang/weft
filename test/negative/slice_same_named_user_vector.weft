-- A user record spelled like the runtime vector is not indexed storage.

type Vector<T> { value: T, other: T }

fn first(view: [i64]) -> i64 { view[0] }

fn main() -> i64 {
  let values = Vector<i64> { value: 7, other: 9 }
  first(values[0..1])
}

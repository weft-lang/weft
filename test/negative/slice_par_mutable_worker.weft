use "stdlib/par.weft"

fn main() -> i64 {
  let mut values = [40, 2]
  let view: [mut i64] = values[..]
  par_with_pthread_pool_i64(2, 4, () => {
    list_fold<i64, i64>(par_map(list_cons<i64>(1, list_nil<i64>()), x => view[0] + x), 0, (acc: i64, x: i64) => acc + x)
  })
}

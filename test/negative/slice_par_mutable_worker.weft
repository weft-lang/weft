use "stdlib/par.weft"

fn main() -> i64 {
  let mut values = [40, 2]
  let view: [mut i64] = values[..]
  let inputs = [1]
  par_with_pthread_pool_i64(2, 4, () => {
    vector_sum_i64(par_map<i64, i64>(inputs[..], x => view[0] + x), 0)
  })
}

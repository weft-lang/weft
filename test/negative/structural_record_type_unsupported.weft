-- 4a S2: structural record types are kernel §3.5, owned by brief 4f.
-- Until then { } in type position must be a diagnostic, not silent any.
fn f(x: {a: str}) -> i64 {
  1
}

fn main() -> i64 {
  0
}

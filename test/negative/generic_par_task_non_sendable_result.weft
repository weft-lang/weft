use stdlib/par.{*}
use stdlib/vector as vector
use stdlib/vector.{*}

fn main() -> i64 {
  par_with_sequential_i64(() => {
    let task = par_spawn<Vector<i64>>(() => vector.new<i64>())
    vector_len<i64>(par_await<Vector<i64>>(task))
  })
}

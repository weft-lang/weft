use stdlib/par as par
use stdlib/par.{*}
use stdlib/vector as vector
use stdlib/vector.{*}

fn main() -> i64 {
  par.with_sequential(() => {
    let task = par.fork<Vector<i64>>(() => vector.new<i64>())
    vector_len<i64>(par.join<Vector<i64>>(task))
  })
}

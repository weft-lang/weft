-- A nilable value used where the non-nil type is required, WITHOUT a
-- guard, must still be rejected: narrowing only fires under a guard.
fn use_str(s: str) -> i64 { __str_len(s) }

fn bad(x: str | nil) -> i64 {
  use_str(x)
}

fn main() -> i64 { bad("hi") }

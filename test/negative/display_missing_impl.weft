use stdlib/display.{*}

type DisplayHidden {
  value: i64
}

fn render<T: Display>(value: T) -> str {
  value.display()
}

fn main() -> i64 {
  let rendered = render<DisplayHidden>(DisplayHidden { value: 42 })
  str_len(rendered)
}

-- A user record spelled like the runtime iterator is not an iteration protocol.

type Iterator<T> { value: T, count: i64 }

fn main() -> i64 {
  let values = Iterator<i64> { value: 7, count: 3 }
  let mut total = 0
  for item in values { total = total + item }
  total
}

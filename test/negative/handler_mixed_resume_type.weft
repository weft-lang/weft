effect MixedResumeType { fn read() -> i64 }

fn bad(cancel: bool) -> str {
  handle { MixedResumeType.read() "completed" } {
    MixedResumeType.read() -> if cancel { "aborted" } else { resume("wrong") }
  }
}

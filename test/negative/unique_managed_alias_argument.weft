type UniqueAliasArgumentBox { value: i64 }

fn consume(value: unique UniqueAliasArgumentBox) -> i64 {
  value.value
}

fn main() -> i64 {
  let value = UniqueAliasArgumentBox { value: 41 }
  let alias = value
  consume(value) + alias.value
}

use stdlib/par as par
use stdlib/par.{*}

fn outside_scope(view: [i64]) -[Par]> i64 {
  let values = [1]
  vector_sum_i64(par.map<i64, i64>(values[..], x => view[0] + x), 0)
}

fn main() -> i64 { 0 }

use stdlib/drop.{*}

type BorrowCaptureToken = opaque i64

impl Drop for BorrowCaptureToken {
  fn drop(self) -> nil { nil }
}

fn inspect(token: borrow BorrowCaptureToken) -> i64 { 1 }

fn bad(token: borrow BorrowCaptureToken) -> i64 {
  let captured = () => inspect(token)
  captured()
}

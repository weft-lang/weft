use test/negative_support/callback_context.{CallbackEntry, CallbackContextHost}

effect CallbackLog { fn read(text: str) -> i64 }

fn main() -[CallbackLog]> i64 {
  CallbackContextHost { value: CallbackEntry { text: "entry" } }.project(entry => CallbackLog.read(entry.text))
}

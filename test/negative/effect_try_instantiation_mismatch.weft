-- qualified names: `?` performs Fail<E> from the Result error type, so a Result whose
-- error is str cannot be propagated by a caller declaring only Fail<i64>.
use stdlib/result.{*}
use stdlib/fail.{*}

fn wrong_atom() -[Fail<i64>]> i64 {
  Err<i64, str>("wrong")?
}

fn main() -> i64 {
  0
}

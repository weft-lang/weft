use compiler/pipeline.{*}

fn main() -> i64 {
  let src = "fn inner() -> i64 { 1 }"
  let typed = pipeline_check_source_with_trust(__str_ptr(src), __str_len(src), 1)
  nlist_len(pipeline_typed_fns(typed))
}

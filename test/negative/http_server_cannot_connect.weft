use stdlib/http.{HttpLimits, HttpRequestHead}
use stdlib/http/endpoint.{HttpClient, HttpClientBegin, HttpServer}
use stdlib/tls/stream.{TlsStream}
use stdlib/url.{Url}

fn connect_with_server_authority(
  url: Url,
  request: HttpRequestHead,
  limits: HttpLimits
) -[HttpServer]> HttpClientBegin<TlsStream> {
  HttpClient<TlsStream>.begin(url, request, limits)
}

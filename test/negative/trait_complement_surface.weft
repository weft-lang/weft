-- 4a S7 (law L1, kernel §2.4): complement over trait atoms is not a surface
-- type — conformance is open-world, so ~Trait would make emptiness
-- non-monotonic under linking.
trait Show {
  fn show(self: i64) -> i64
}

fn f(x: i64 & ~Show) -> i64 {
  1
}

fn main() -> i64 {
  0
}

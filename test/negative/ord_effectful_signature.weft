effect OrdNoise {
  fn tick() -> nil
}

trait Ord {
  fn compare(self: Self, other: Self) -[OrdNoise]> i64
}

type NoisyOrd {
  NoisyOrd(i64)
}

impl Ord for NoisyOrd {
  fn compare(self: Self, other: Self) -> i64 { 0 }
}

fn main() -> bool {
  NoisyOrd(1) < NoisyOrd(2)
}

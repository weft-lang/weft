use stdlib/continuation.{*}

effect StoredK {
  @deferred fn get() -> i64
}

fn main() -> i64 {
  let cell = continuation_cell_new()
  handle StoredK.get() {
    StoredK.get() with k -> {
      let ok = continuation_cell_store_i64(cell, k)
      k(ok)
    }
  }
}

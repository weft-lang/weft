type BorrowTemporaryManagedValue {
  text: str
}

fn inspect(value: borrow BorrowTemporaryManagedValue) -> i64 {
  value.text.len()
}

fn main() -> i64 {
  inspect(BorrowTemporaryManagedValue { text: "temporary" })
}

trait QualifiedReceiver {
  fn consume(self: Self) -> i64
}

type QualifiedReceiverBox {
  value: i64
}

impl QualifiedReceiver for unique QualifiedReceiverBox {
  fn consume(self: QualifiedReceiverBox) -> i64 { self.value }
}

fn main() -> i64 { 0 }

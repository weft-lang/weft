-- abort cleanup keeps the deferred-continuation completion-transform fence explicit.
effect DeferredReturn {
  @deferred fn get() -> i64
}

fn main() -> i64 {
  handle DeferredReturn.get() + 1 {
    DeferredReturn.get() with k -> k(42)
    return value -> value
  }
}

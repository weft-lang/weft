use stdlib/drop.{*}

type BorrowEscapeToken = opaque i64

impl Drop for BorrowEscapeToken {
  fn drop(self) -> nil { nil }
}

fn consume(token: owned BorrowEscapeToken) -> i64 { 0 }
fn bad(token: borrow BorrowEscapeToken) -> i64 { consume(token) }

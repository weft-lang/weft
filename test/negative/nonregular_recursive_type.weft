use stdlib/vector/handles.{Vector}

-- Each level of the chain demands a larger instantiation of the chain.
type Chain<T> { ChainEnd(T), ChainDeeper(Chain<Vector<T>>) }

type Node { label: i64, chain: Chain<Node> }

fn main() -> i64 { 0 }

trait AssocDuplicateTrait {
  type Item
  fn next(self: i64) -> i64
}

type AssocDuplicateBox {
  v: i64
}

impl AssocDuplicateTrait for AssocDuplicateBox {
  type Item = i64
  type Item = str

  fn next(self: i64) -> i64 {
    __mem_load64(self)
  }
}

use stdlib/diagnostic/registry as registry
use stdlib/diagnostic/registry.{DiagnosticRegistryEntry}
use stdlib/option.{Option}

fn signed_index(index: i64) -> Option<DiagnosticRegistryEntry> { registry.get(index) }

fn main() -> i64 { 0 }

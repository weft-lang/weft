use stdlib/iter as iter
use stdlib/option.{*}

fn main() -> usize {
  let source = iter.range(0, 3)
  let mapped = iter.filter_map<i64, i64>(source, value => Some<i64>(value))
  let original_count = iter.count<i64>(source)
  original_count + iter.count<i64>(mapped)
}

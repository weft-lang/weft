trait QualifiedBorrowTarget {
  fn inspect(self: Self) -> i64
}

type QualifiedBorrowBox {
  value: i64
}

impl QualifiedBorrowTarget for borrow QualifiedBorrowBox {
  fn inspect(self: borrow QualifiedBorrowBox) -> i64 { self.value }
}

fn main() -> i64 { 0 }

-- imported_got_support.weft - Darwin GOT helper imported by a test.

use runtime/tool_platform.{runtime_libsystem_write_fixture}
use runtime/unsafe.{Unsafe}

pub(package) fn imported_got_write(msg: i64, len: i64) -[Unsafe]> i64 {
  runtime_libsystem_write_fixture(1, msg, len)
}

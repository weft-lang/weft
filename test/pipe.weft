-- Pipe operator: x |> f(y) desugars to f(x, y)
fn add(a: Int, b: Int) -> Int { a + b }
fn mul(a: Int, b: Int) -> Int { a * b }

fn main() {
  -- 3 |> add(4) |> mul(2) = mul(add(3, 4), 2) = 7 * 2 = 14
  3 |> add(4) |> mul(2)
}

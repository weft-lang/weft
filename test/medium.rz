type Option {
  Some(Int),
  None
}

fn abs(x: Int) -> Int {
  if x < 0 {
    -x
  } else {
    x
  }
}

fn classify(x: Int) -> String {
  match x {
    0 -> "zero",
    1 -> "one",
    _ -> "other"
  }
}

fn main() {
  let result = abs(-5)
  result
}

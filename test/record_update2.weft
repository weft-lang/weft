-- Test: rebuilt record preserves unchanged fields
-- Expected exit code: 20

type Point {
  x: i64,
  y: i64
}

fn main() -> i64 {
  let p = Point { x: 10, y: 20 }
  let q = Point { x: 42, y: p.y }
  q.y
}

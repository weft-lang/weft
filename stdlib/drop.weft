-- stdlib/drop.weft — resource cleanup contract for owned values

-- Drop is a marker trait for owned resource cleanup. Implementing types provide
-- a concrete `drop` method; the compiler reads that method's actual effect set
-- when deciding whether an owned value may go out of scope.
--- Marks an owned resource whose implementation provides deterministic cleanup.
pub trait Drop { }

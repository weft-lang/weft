-- stdlib/panic.weft -- ordinary panic surface

use runtime/panic.{*}

--- Terminate the current program with `msg`; this function never returns.
pub fn panic(msg: str) -> never {
  runtime_panic(msg)
}

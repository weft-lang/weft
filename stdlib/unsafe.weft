-- stdlib/unsafe.weft -- retired public unsafe surface
--
-- The actual Unsafe effect operations and helper wrappers now live in
-- `runtime/unsafe.weft`, which is part of the trusted runtime/platform TCB.
-- This stdlib module intentionally exports no operations: ordinary packages
-- cannot opt into raw memory by importing a public `unsafe` module.

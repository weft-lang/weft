-- stdlib/display.weft — Display trait for human-readable string conversion
--
-- Display provides a standard way to convert values to strings.
-- Used by string interpolation (when implemented) and print functions.

use stdlib/num.{num_f32_to_str, num_f64_to_str}
use stdlib/string.{*}

pub trait Display {
  fn display(self) -> str
}

impl Display for i64 {
  fn display(self: i64) -> str {
    int_to_str(self)
  }
}

impl Display for bool {
  fn display(self: bool) -> str {
    if self == 1 { "true" } else { "false" }
  }
}

impl Display for str {
  fn display(self: str) -> str {
    self
  }
}

impl Display for f64 {
  fn display(self: f64) -> str {
    num_f64_to_str(self)
  }
}

impl Display for f32 {
  fn display(self: f32) -> str {
    num_f32_to_str(self)
  }
}

-- Print any Display value to stdout
pub fn print_val<T: Display>(x: T) -[IO]> i64 {
  print(x.display())
}

-- Print any Display value followed by newline
pub fn println_val<T: Display>(x: T) -[IO]> i64 {
  println(x.display())
}

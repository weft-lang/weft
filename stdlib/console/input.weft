-- stdlib/console/input.weft — host standard-input interpretation

use runtime/console_read.{runtime_safe_console_read}
use stdlib/console.{ConsoleRead}

--- Install host standard input as the interpretation of `ConsoleRead`.
pub implements ConsoleRead() {
  ConsoleRead.read(limit) -> resume(runtime_safe_console_read(limit))
}

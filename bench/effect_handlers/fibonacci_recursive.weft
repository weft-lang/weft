-- effect-handlers-bench: fibonacci_recursive (spec large input 42 -> 433494437)
-- Pure doubly-recursive fibonacci: measures call-stack cost, no effects.

fn fib(n: i64) -> i64 {
  if n < 2 { 1 } else { fib(n - 1) + fib(n - 2) }
}

fn main() -> i64 {
  if fib(42) == 433494437 { 0 } else { 1 }
}

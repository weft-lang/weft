-- binary_format.weft — target binary format descriptors

type BinaryFormat {
  MachOExecutable,
  MachOObject,
  ElfExecutable,
  ElfObject,
  UnsupportedBinaryFormat
}

type BinaryFormatBackend {
  BinaryFormatBackendUnsupported,
  BinaryFormatBackendMachOExecutable,
  BinaryFormatBackendMachOObject
}

fn binary_format_macho_executable() -> BinaryFormat { MachOExecutable }
fn binary_format_macho_object() -> BinaryFormat { MachOObject }
fn binary_format_elf_executable() -> BinaryFormat { ElfExecutable }
fn binary_format_elf_object() -> BinaryFormat { ElfObject }
fn binary_format_unsupported() -> BinaryFormat { UnsupportedBinaryFormat }

fn binary_format_backend_unsupported() -> BinaryFormatBackend { BinaryFormatBackendUnsupported }
fn binary_format_backend_macho_executable() -> BinaryFormatBackend { BinaryFormatBackendMachOExecutable }
fn binary_format_backend_macho_object() -> BinaryFormatBackend { BinaryFormatBackendMachOObject }

fn binary_format_backend_id(backend: BinaryFormatBackend) -> i64 {
  match backend {
    BinaryFormatBackendUnsupported -> 0
    BinaryFormatBackendMachOExecutable -> 1
    BinaryFormatBackendMachOObject -> 2
  }
}

fn binary_format_is_supported(format: BinaryFormat) -> i64 {
  match format {
    MachOExecutable -> 1
    MachOObject -> 1
    ElfExecutable -> 0
    ElfObject -> 0
    UnsupportedBinaryFormat -> 0
  }
}

fn binary_format_backend(format: BinaryFormat) -> BinaryFormatBackend {
  match format {
    MachOExecutable -> binary_format_backend_macho_executable()
    MachOObject -> binary_format_backend_macho_object()
    ElfExecutable -> binary_format_backend_unsupported()
    ElfObject -> binary_format_backend_unsupported()
    UnsupportedBinaryFormat -> binary_format_backend_unsupported()
  }
}

fn binary_format_default_for_relocations(relocations: i64) -> BinaryFormat {
  if relocations > 0 {
    binary_format_macho_object()
  } else {
    binary_format_macho_executable()
  }
}

fn binary_format_kind(format: BinaryFormat) -> i64 {
  match format {
    MachOExecutable -> 1
    MachOObject -> 2
    ElfExecutable -> 3
    ElfObject -> 4
    UnsupportedBinaryFormat -> 0
  }
}

fn binary_format_name_len_or(format: BinaryFormat, default: i64) -> i64 {
  match format {
    MachOExecutable -> __str_len("macho-executable")
    MachOObject -> __str_len("macho-object")
    ElfExecutable -> __str_len("elf-executable")
    ElfObject -> __str_len("elf-object")
    UnsupportedBinaryFormat -> default
  }
}

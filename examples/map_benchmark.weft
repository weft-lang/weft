-- CHAMP map benchmark: insert N keys, look up all, count
-- Benchmark: persistent data structure performance, hashing, allocation
use "stdlib/map.weft"

fn bench_map(n: i64) -> i64 {
  -- Insert n key-value pairs
  let mut m = map_new()
  for i in 0..n {
    m = map_put(m, i, i * i)
  }
  -- Verify all keys present
  let mut found: i64 = 0
  for i in 0..n {
    if map_contains(m, i) == 1 { found = found + 1 } else { 0 }
  }
  -- Verify values correct
  let mut correct: i64 = 0
  for i in 0..n {
    if map_get_or(m, i, 0 - 1) == i * i { correct = correct + 1 } else { 0 }
  }
  let size = map_size(m)
  if found == n and correct == n and size == n { 0 } else { 1 }
}

fn main() -> i64 {
  bench_map(1000)
}
